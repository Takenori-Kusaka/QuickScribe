QuickScribe — 第三者ライセンス帰属表記 (THIRD-PARTY-NOTICES)
================================================================================
本ソフトウェアは以下のオープンソースコンポーネントを利用しています。各コンポーネントの
著作権およびライセンス全文を以下に掲げます（cargo-about により Cargo.lock から自動生成）。

{{#each licenses}}
================================================================================
{{name}}{{#if id}} ({{id}}){{/if}}
================================================================================
Used by:
{{#each used_by}}
  - {{crate.name}} {{crate.version}}{{#if crate.repository}} ({{crate.repository}}){{/if}}
{{/each}}

{{{text}}}

{{/each}}
