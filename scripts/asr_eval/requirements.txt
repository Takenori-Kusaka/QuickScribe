# 日本語 ASR 評価スタック（ADR-0024 / #578・#577）。**CI/評価でのみ使用**。アプリ本体(Tauri/Rust)は非依存。
# 第一スライス（評価コア）は標準ライブラリのみで動く。neologdn は任意（未導入時は正規化の該当段をスキップ）。
neologdn>=0.5.4          # 日本語正規化（長音短縮/ダッシュ統一/繰り返し短縮/半角カナ）。C拡張。

# --- 後続スライスで追加予定（コメントで先出し）---
# datasets>=2.19,<3     # Common Voice ja(CC0ミラー)取得（#578）。fsicoliミラーはスクリプト方式で
#                        # datasets 4.0+ は trust_remote_code 廃止のため 2.x にピン必須。
# bert-score>=0.3.13     # 意味保持 BERTScore-ja（#577）
# transformers>=4.40     # 日本語NLI entailment / BERTScore（#577）
# torch>=2.2             # 上記モデルのCPU推論（TRANSFORMERS_OFFLINE=1でオフライン実行）
